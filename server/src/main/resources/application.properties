# Spring datasource
spring.datasource.url=jdbc:mysql://localhost:3306/qrmatic?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=testere1500
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Server
server.port=8080

# Logging
logging.level.org.hibernate.SQL=DEBUG
logging.level.com.qrmatik.server=DEBUG

# File uploads directory (inside server/)
app.upload-dir=./uploads
