package com.qrmatik.server.controller;

/**
 * CallbackController removed: payment provider integration was removed and
 * callbacks are no longer used. This class intentionally has no request
 * mappings to avoid exposing old endpoints.
 */
public final class CallbackController {
    // Intentionally empty. Kept so code referencing the class path doesn't
    // fail if any code imports it. Delete entirely if you prefer.
}
